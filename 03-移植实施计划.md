# MeloX-Windows — 移植实施计划

> 目标：以最小成本将 MeloX-Android 的业务逻辑带到 Windows
> 方案：Compose Multiplatform Desktop + 共享 Kotlin 模块
> 参考：MeloXDesktop（macOS）的适配模式

---

## 零、前置条件

- JDK 17+
- Gradle 8.x+
- Kotlin Multiplatform 插件
- Compose Multiplatform（JetBrains）
- Windows 10/11 开发环境（Windows SDK）

---

## 阶段一：工程搭建（1-2 天）

### 1.1 创建 Gradle 工程

```
MeloX-Windows/
├── build.gradle.kts              # 根构建文件
├── settings.gradle.kts           # 项目设置
├── shared/                       # 共享模块
│   ├── build.gradle.kts          # Kotlin Multiplatform
│   │   ├── commonMain            # 纯 Kotlin（无平台依赖）
│   │   ├── androidMain           # Android 实现（可选，后续接入）
│   │   └── desktopMain           # Desktop 实现
├── desktop/                      # Windows 应用模块
│   ├── build.gradle.kts          # Compose Multiplatform Desktop
│   └── src/main/kotlin/
└── gradle/
    └── libs.versions.toml        # 版本目录
```

### 1.2 配置依赖

```kotlin
// shared/build.gradle.kts
kotlin {
    jvm("desktop")  // Windows 目标
    
    sourceSets {
        val commonMain by getting
        val desktopMain by getting {
            dependencies {
                implementation(compose.ui)
                implementation(compose.foundation)
                implementation(compose.material3)
                implementation(compose.uiToolingPreview)
                implementation("org.jetbrains.kotlinx:kotlinx-coroutines-core:1.10.2")
                implementation("io.ktor:ktor-client-core:3.x")
                implementation("io.ktor:ktor-client-cio:3.x")
                implementation("org.jetbrains.kotlinx:kotlinx-datetime:0.7.x")
                implementation("com.russhwolfSettings:multiplatform-settings:1.x")
            }
        }
    }
}

// desktop/build.gradle.kts
kotlin {
    jvm("desktop") {
        jvmToolchain(17)
    }
    
    sourceSets {
        val desktopMain by getting {
            dependencies {
                implementation(project(":shared"))
                implementation(compose.desktop.current)
                implementation("org.jetbrains.kotlinx:kotlinx-coroutines-core:1.10.2")
                implementation("org.jetbrains.kotlinx:kotlinx-coroutines-javawindow")
            }
        }
    }
}
```

---

## 阶段二：共享模块抽取（3-5 天）

### 2.1 创建目录结构

在 `shared/commonMain/` 下按 Android 的 `core/` 结构创建包：

```
shared/commonMain/
├── lyrics/
│   ├── LyricModels.kt                    ← 复制自 core/lyrics/
│   ├── LrcLyricsParser.kt
│   ├── KugouKrcLyricsParser.kt
│   ├── QQMusicQrcLyricsParser.kt
│   ├── QQMusicMeiQrcDecoder.kt
│   ├── TtmlLyricsParser.kt
│   ├── AmlldbLyricsClient.kt             ← 改 OkHttp → Ktor
│   ├── LyricTimelineProcessor.kt
│   ├── LyricRomanizationAligner.kt
│   ├── LyricBindingStore.kt              ← 改 Context → 文件路径
│   └── MeloXProviderLyricsLoader.kt      ← 改 Context → 文件路径
├── music/
│   ├── model/
│   │   ├── MusicModels.kt
│   │   ├── SearchSong.kt
│   │   └── ...
│   └── provider/
│       ├── MusicProvider.kt
│       ├── MeloXMusicProviders.kt
│       ├── MusicCapability.kt
│       ├── UnifiedMusicService.kt
│       └── ...
├── network/
│   ├── NeteaseSearchClient.kt            ← 改 OkHttp → Ktor
│   ├── NeteaseAuthenticatedEapi.kt       ← 改 OkHttp → Ktor
│   ├── NeteaseAuthenticatedWeapi.kt      ← 改 OkHttp → Ktor
│   ├── NeteaseMusicOperationsClient.kt
│   ├── NeteaseCollectionDetailsClient.kt
│   ├── NeteaseSocialExtrasClient.kt
│   ├── NeteaseUniversalSearchClient.kt
│   ├── NeteasePhoneAuthClient.kt
│   ├── NeteaseListenTogetherTransport.kt
│   ├── NeteaseListenTogetherInvitation.kt
│   ├── MeloXHttpClient.kt                ← 改 OkHttp → Ktor
│   ├── MeloXNetworkAvailability.kt       ← 改 ConnectMonitor
│   └── MeloXGitHubRouting.kt
├── provider/
│   ├── netease/NeteaseProvider.kt
│   ├── qqmusic/*.kt
│   ├── kugou/*.kt
│   ├── spotify/*.kt
│   ├── jellyfin/*.kt
│   ├── bilibili/*.kt
│   ├── kuwo/*.kt
│   ├── local/*.kt                        ← 改 MediaStore → 文件 API
│   ├── applemusic/*.kt                   ← 去掉 Apple Music SDK 依赖
│   └── lxuser/*.kt
├── audio/
│   ├── MusicQuality*.kt
│   └── ...
├── playback/
│   ├── MeloXAutoMix.kt
│   ├── MeloXAutoMixAudioAnalyzer.kt
│   ├── MeloXAutoMixEqualizerEnvelope.kt
│   ├── MeloXEqualizerController.kt
│   ├── ProviderPlaybackResolver.kt
│   ├── ChkszPlaybackResolver.kt
│   ├── LxUserPlaybackResolver.kt
│   ├── NeteasePlaybackResolver.kt
│   ├── MeloXMediaPrefetcher.kt
│   ├── MeloXMediaCache.kt               ← 改文件路径
│   ├── MeloXAudioReactiveRuntime.kt
│   ├── MeloXAudioAnalysisRuntime.kt
│   ├── MeloXAudioAnalysisStore.kt
│   ├── MeloXListenedTimeTracker.kt
│   ├── MeloXPlaybackHistoryReporter.kt
│   ├── MeloXListenTogetherCoordinator.kt
│   ├── MeloXPlayerTransitionState.kt
│   ├── PlaybackCommands.kt
│   ├── ProviderPlaybackCommands.kt
│   ├── PlaybackTrackIdentity.kt
│   ├── ProviderPlaybackQualityRuntime.kt
│   ├── MeloXPlaybackModePreferences.kt
│   └── CrossProviderPlaybackFallback.kt
├── account/
│   ├── NeteaseSessionStore.kt            ← 改 Cookie 存储
│   └── ...
├── download/
│   └── MeloXDownloadStore.kt             ← 改 JSON 路径
├── recognition/
│   └── *.kt                              ← 音频 API 适配
├── recommendation/
│   └── *.kt
├── update/
│   └── MeloXUpdateClient.kt
├── diagnostics/
│   └── MeloXLogExporter.kt
├── remoteconfig/
│   └── *.kt
└── model/
    └── SearchSong.kt
```

### 2.2 改动清单

每个复制的文件需要：

1. **package 声明**：`package com.lladlam.melox.core.xxx` → `package melox.xxx`
2. **import 路径**：更新内部引用
3. **OkHttp → Ktor**（仅网络相关文件）：
   - `okhttp3.*` → `io.ktor.client.*`
   - `OkHttpClient` → `HttpClient(CIO)`
   - `Request.Builder()` → `HttpRequestBuilder`
   - `Response.body().string()` → `bodyAsText()`
4. **Context → 路径**（仅存储相关文件）：
   - `context.getFilesDir()` → 文件系统路径
   - `contextSharedPreferences` → Multiplatform Settings
5. **Android 特有 API**：
   - `Build.VERSION` → `System.getProperty("java.version")` 或 expect/actual
   - `Log.d()` → `println()` 或 expect/actual 日志

---

## 阶段三：音频后端（5-7 天）

### 3.1 选型

推荐使用 **Juce**（Kotlin/JVM 绑定），功能最接近 Media3：

| 需求 | Media3 | Juce |
|------|--------|------|
| 播放/暂停/停止 | ✅ | ✅ |
| 进度控制 | ✅ | ✅ |
| 音量控制 | ✅ | ✅ |
| 缓存 | ✅ (CacheDataSource) | ✅ (Juce AudioFormatManager) |
| 预加载 | ✅ (MediaPrefetcher) | ⚠️ 需双实例 |
| 淡入淡出 | ✅ | ✅ |
| AutoMix 双播放器 | ✅ (incomingPlayer) | ⚠️ 需双实例 |
| MediaSession | ✅ | ❌ 需自建 |

### 3.2 实现

```kotlin
// desktopMain/melox/playback/WindowsAudioEngine.kt
class WindowsAudioEngine {
    private val primary = JuceAudioPlayer()      // 主播放器
    private val secondary = JuceAudioPlayer()    // AutoMix 预载
    
    fun play(url: String, positionMs: Long = 0) { ... }
    fun pause() { ... }
    fun seekTo(positionMs: Long) { ... }
    fun setVolume(volume: Float) { ... }
    fun release() { ... }
    
    // AutoMix 双播放器
    fun prepareSecondary(url: String) { ... }
    fun transitionToSecondary() { ... }
}
```

### 3.3 AutoMix 双播放器适配

Android 侧 `MeloXPlaybackService.kt` 有 `player` + `incomingPlayer` 两个 ExoPlayer 实例。
Windows 侧需要两个独立的音频播放实例，在过渡时无缝切换。

---

## 阶段四：Desktop UI（10-15 天）

### 4.1 设计参考

参考 macOS Desktop 的布局模式，但用 Compose Multiplatform Desktop 实现：

| macOS Desktop 文件 | Windows 对应 | 功能 |
|-------------------|-------------|------|
| `DesktopRootView.swift` | `WindowsRootView.kt` | 根导航 |
| `DesktopNowPlayingLayout.swift` | `WindowsNowPlaying.kt` | 播放器 |
| `DesktopLyricsScrollView.swift` | `WindowsLyrics.kt` | 歌词 |
| `DesktopMiniPlayerWindow.swift` | `WindowsMiniPlayer.kt` | MiniPlayer |
| `DesktopFloatingLyricsWindow.swift` | `WindowsFloatingLyrics.kt` | 悬浮歌词 |
| `DesktopCommands.swift` | `WindowsCommands.kt` | 快捷键 |

### 4.2 页面清单

| 页面 | 功能 | 复杂度 |
|------|------|--------|
| `WindowsRootView.kt` | 根导航 + 底栏 | 中 |
| `WindowsHome.kt` | 首页 | 高 |
| `WindowsExplore.kt` | 发现页 | 中 |
| `WindowsSearch.kt` | 搜索 | 高 |
| `WindowsLibrary.kt` | 音乐库 | 高 |
| `WindowsNowPlaying.kt` | 全屏播放器 | 最高 |
| `WindowsMiniPlayer.kt` | MiniPlayer | 中 |
| `WindowsLyrics.kt` | 歌词面板 | 高 |
| `WindowsQueue.kt` | 播放队列 | 中 |
| `WindowsSettings.kt` | 设置 | 高 |
| `WindowsFloatingLyrics.kt` | 悬浮歌词 | 中 |
| `WindowsPlayerInspector.kt` | 播放器检查器 | 低 |

### 4.3 关键复用

- **歌词渲染**：`MeloXIOSLyricsPanel.kt`（2418行）的 Canvas 绘制逻辑可直接移植到 Compose Multiplatform Desktop
- **玻璃效果**：AndroidLiquidGlass → Compose Multiplatform 原生 `Backdrop` + `Blur`
- **动画**：Compose 动画 API 在 Desktop 上完全兼容

---

## 阶段五：平台能力（3-5 天）

### 5.1 窗口管理

```kotlin
// Windows 多窗口配置
fun main() = application {
    Window(
        title = "MeloX",
        state = rememberWindowState(),
        onCloseRequest = ::exitApplication,
    ) {
        MeloXWindowsApp()
    }
    
    // MiniPlayer 窗口
    Window(
        title = "",
        state = rememberWindowState(),
        alwaysOnTop = true,
        transparentBackground = true,
    ) {
        WindowsMiniPlayer()
    }
    
    // 悬浮歌词窗口
    Window(
        title = "",
        state = rememberWindowState(),
        alwaysOnTop = true,
        transparentBackground = true,
        undecorated = true,
    ) {
        WindowsFloatingLyrics()
    }
}
```

### 5.2 系统托盘

```kotlin
fun createTray() {
    // 使用 Compose Desktop 的 Tray API
    Tray(icon = "tray_icon.png") {
        Item("播放/暂停") { player.togglePlay() }
        Item("上一首") { player.previous() }
        Item("下一首") { player.next() }
        Separator()
        Item("退出") { exitApplication() }
    }
}
```

### 5.3 全局快捷键

参考 macOS `DesktopPlaybackKeyboardShortcuts.swift`：
- `Space` — 播放/暂停
- `←/→` — 上一首/下一首
- `↑/↓` — 音量加减
- `Cmd+Shift+F` — 搜索

### 5.4 存储

```kotlin
// expect/actual 存储
expect class MeloXStorage {
    val dataDir: String
    fun readString(key: String): String?
    fun writeString(key: String, value: String)
    fun delete(key: String)
}
```

---

## 阶段六：打包（2-3 天）

### 6.1 配置

```kotlin
// desktop/build.gradle.kts
compose.desktop {
    application {
        mainClass = "melox.WindowsAppKt"
        nativeDistributions {
            targetFormats(TargetFormat.Dmg, TargetFormat.Msi, TargetFormat.Exe)
            packageVersion = "1.0.0"
            includeModulesInDigest = true
        }
    }
}
```

### 6.2 生成 Windows 安装包

```bash
./gradlew desktopPackageReleaseUbi
# 输出: build/compose/desktop/release/desktop-ubi.exe
```

或使用 jpackage 生成原生 Windows installer。

---

## 七、里程碑

| 里程碑 | 内容 | 预计完成 |
|--------|------|---------|
| M1 | 工程搭建 + 共享模块编译通过 | 第 3 天 |
| M2 | 歌词引擎在 Desktop 运行 | 第 8 天 |
| M3 | 音频后端可用（播放/暂停/进度） | 第 15 天 |
| M4 | 播放器 UI 可用 | 第 30 天 |
| M5 | 完整应用（首页/搜索/库/设置） | 第 45 天 |
| M6 | 打包 Windows installer | 第 50 天 |

---

## 八、风险登记

| 风险 | 影响 | 缓解 |
|------|------|------|
| JVM 音频库功能不足 | AutoMix/预加载不可用 | 优先验证 Juce，备选 Tritone |
| Compose Multiplatform 稳定性 | UI  bug | 及时更新到最新版 |
| QQ/酷狗签名算法 | 部分 Provider 不可用 | 保留 Android 侧实现作为参考 |
| 悬浮歌词窗口 | Windows 无边框透明 | 测试不同窗口配置 |
| 构建产物过大 | JVM 应用体积大 | 使用 jlink 剪裁 |