# MeloX Windows 移植 — 三平台架构分析

> 生成时间：2026-09-07
> 分析对象：MeloX iOS（moye）、MeloXDesktop macOS（moye）、MeloX-Android（用户移植）
> 目标：以最小成本将 MeloX 带到 Windows

---

## 一、三个平台的代码规模

| 平台 | 语言 | 文件数 | 总行数 | 开发者 |
|------|------|--------|--------|--------|
| MeloX iOS | Swift | 375 | ~77,553 | moye |
| MeloXDesktop | Swift | 284 | ~59,047 | moye（基于 iOS Core） |
| MeloX-Android | Kotlin | ~200+ | ~50,000+ | 用户（移植自 iOS） |

---

## 二、iOS / Desktop 的架构模式（关键参考）

### 2.1 iOS 分层

```
MeloX iOS
├── App/                          # 应用入口、导航、启动体验
│   ├── MeloXApp.swift            # @main struct，持有所有 Store
│   ├── ContentView.swift         # 根视图 + 标签页导航
│   ├── AppNavigation.swift       # 导航过渡
│   └── AppLaunchExperience.swift # 启动屏
├── Core/                         # 核心业务（全部纯 Swift，平台无关逻辑）
│   ├── Playback/                 # 播放引擎、队列、AutoMix、均衡器、Session
│   │   ├── Engine/               # AudioPlaybackEngine（AVFoundation 封装）
│   │   ├── Session/PlayerStore.swift (1821行)  # 播放状态中心
│   │   ├── Queue/
│   │   ├── AutoMix/
│   │   └── Equalizer/
│   ├── Lyrics/                   # 歌词引擎
│   │   ├── LyricSourceMerger.swift  # 多源聚合（AMLL→NetEase→QQ）
│   │   ├── TTMLLyricParser.swift    # TTML 解析
│   │   ├── LyricParser.swift        # LRC/YRC 解析
│   │   ├── LyricStore.swift         # 歌词状态
│   │   └── LyricDuetParser.swift    # 对唱支持
│   ├── Networking/               # 网易云 API 客户端
│   │   ├── NeteaseAPI.swift (1017行)
│   │   ├── NeteaseDirectClient.swift
│   │   └── NeteaseSocialAPI.swift
│   ├── Models/                   # 领域模型
│   │   ├── Music/MusicModels.swift
│   │   ├── Account/
│   │   ├── Home/
│   │   ├── Social/
│   │   └── Podcast/
│   ├── Settings/                 # 设置（AppSettings + 分模块偏好）
│   ├── Storage/                  # 本地存储
│   ├── Artwork/                  # 封面处理
│   ├── Library/                  # 音乐库
│   ├── Cloud/                    # 云盘
│   ├── Downloads/                # 下载
│   ├── Notifications/            # 歌词通知
│   ├── LiveActivity/             # Live Activity / Dynamic Island
│   ├── Watch/                    # Apple Watch 同步
│   └── System/                   # 系统能力（屏幕唤醒等）
├── Features/                     # UI 功能模块
│   ├── Player/                   # 播放器 UI（全屏、Mini、歌词、队列）
│   ├── Home/ Explore/ Search/ Library/ ...
│   └── Settings/
├── Shared/                       # 共享组件
│   ├── Components/
│   ├── Media/                    # 封面组件、卡片布局
│   └── ...
└── Resources/                    # 资源文件
```

### 2.2 macOS Desktop 的适配模式（关键！）

Desktop **不是重写**，而是在 iOS Core 之上加了一个 Desktop 适配层：

```
MeloXDesktop
├── App/                          # Desktop 适配层（新增）
│   ├── MeloXDesktopApp.swift    # @main，多窗口场景配置
│   ├── DesktopAppModel.swift    # 包装 iOS Store 的 Desktop Model
│   ├── DesktopRootView.swift    # Desktop 根视图
│   ├── DesktopNavigation.swift  # Desktop 导航
│   ├── DesktopCommands.swift    # 菜单栏快捷键
│   ├── DesktopMiniPlayerWindow.swift  # MiniPlayer 独立窗口
│   ├── DesktopFloatingLyricsWindow.swift  # 悬浮歌词窗口
│   └── DesktopPlaybackKeyboardShortcuts.swift  # 全局快捷键
├── Core/                         # 与 iOS 几乎相同的 Core
│   └── （从 iOS Core 复制/共享）
├── Features/                     # 从 iOS Features 适配
│   ├── Player/                   # Desktop 播放器布局
│   │   ├── DesktopNowPlayingLayout.swift
│   │   ├── DesktopLyricsScrollView.swift
│   │   ├── DesktopNowPlayingArtwork.swift
│   │   ├── DesktopNowPlayingBackground.swift
│   │   └── ...
│   ├── Sidebar/                  # Desktop 侧边栏导航
│   └── ...
└── Shared/                       # Desktop 适配组件
    ├── DesktopArtworkView.swift
    ├── DesktopTrackRow.swift
    └── DesktopMediaCard.swift
```

**Desktop 的核心设计模式**：
1. `DesktopAppModel` 包装 iOS 的 `PlayerStore`、`LyricsStore`、`NeteaseAPI` 等
2. Desktop 专属功能（多窗口、菜单栏、快捷键）封装在 `Desktop*` 文件中
3. iOS Core 代码几乎不修改，直接复用
4. UI 布局用 Desktop 专属 Composable 适配

**这就是 Windows 移植最该参考的模式。**

---

## 三、Android 的架构（用户移植）

### 3.1 分层

```
MeloX-Android
├── android/app/src/main/
│   ├── kotlin/com/lladlam/melox/
│   │   ├── core/                # 核心业务（对应 iOS Core）
│   │   │   ├── lyrics/          # 歌词引擎（TTML + 多源聚合）
│   │   │   ├── music/           # 领域模型 + Provider 抽象
│   │   │   │   ├── model/       # MusicModels, MusicResourceId, MusicSource
│   │   │   │   └── provider/    # MusicProvider + 各 Provider 实现
│   │   │   ├── network/         # 网络客户端（OkHttp）
│   │   │   ├── provider/        # 各音乐源实现
│   │   │   │   ├── netease/     # 网易云
│   │   │   │   ├── qqmusic/     # QQ音乐
│   │   │   │   ├── kugou/       # 酷狗
│   │   │   │   ├── spotify/     # Spotify
│   │   │   │   ├── jellyfin/    # Jellyfin
│   │   │   │   ├── bilibili/    # B站
│   │   │   │   ├── applemusic/  # Apple Music
│   │   │   │   ├── kuwo/        # 酷我
│   │   │   │   ├── local/       # 本地音乐
│   │   │   │   └── lxuser/      # LX Music 源
│   │   │   ├── playback/        # 播放相关
│   │   │   ├── account/         # 账号/session
│   │   │   ├── download/        # 下载管理
│   │   │   ├── audio/           # 音质/均衡器
│   │   │   ├── library/         # 音乐库
│   │   │   ├── recognition/     # 听歌识曲
│   │   │   ├── remoteconfig/    # 远程配置
│   │   │   ├── recommendation/  # 推荐引擎
│   │   │   ├── update/          # 版本检查
│   │   │   ├── diagnostics/     # 日志导出
│   │   │   └── ...
│   │   ├── playback/            # Media3 播放服务（Android 平台封装）
│   │   │   ├── MeloXPlaybackService.kt (1499行)  # MediaSessionService
│   │   │   ├── MeloXAutoMix.kt
│   │   │   ├── MeloXEqualizerController.kt
│   │   │   └── ...
│   │   ├── ui/                  # Compose UI
│   │   │   ├── MeloXApp.kt (1298行)  # 应用入口
│   │   │   ├── player/          # 播放器 UI
│   │   │   │   ├── MeloXIOSLyricsPanel.kt (2418行)
│   │   │   │   ├── MeloXPlayerUi.kt (1214行)
│   │   │   │   ├── MeloXIOSNowPlayingScene.kt (739行)
│   │   │   │   └── ...
│   │   │   ├── library/         # 音乐库 UI
│   │   │   ├── search/          # 搜索 UI
│   │   │   ├── settings/        # 设置 UI
│   │   │   ├── glass/           # 液态玻璃组件
│   │   │   └── ...
│   │   └── platform/            # 平台适配
│   │       ├── floating/        # 悬浮歌词 Service
│   │       ├── xiaomi/          # HyperOS 适配
│   │       └── lyricon/         # 歌词 Provider 桥接
│   └── res/                     # Android 资源
```

### 3.2 Android 与 iOS 的对应关系

| Android 模块 | iOS 对应 | 可移植性 |
|-------------|---------|---------|
| `core/lyrics/` | `Core/Lyrics/` | ✅ 100% 纯 Kotlin |
| `core/music/model/` | `Core/Models/Music/` | ✅ 100% 纯 Kotlin |
| `core/music/provider/` | 无直接对应（iOS 单源） | ✅ 100% 纯 Kotlin |
| `core/network/` | `Core/Networking/` | ⚠️ OkHttp → 需换 Ktor |
| `core/provider/netease/` | `Core/Networking/NeteaseAPI.swift` | ⚠️ HTTP 签名可复用 |
| `core/provider/qqmusic/` | `Core/Lyrics/QQMusicLyricDecoder.swift` | ⚠️ 同上 |
| `core/audio/` | `Core/Playback/Equalizer/` | ✅ 纯算法 |
| `playback/MeloXAutoMix.kt` | `Core/Playback/AutoMix/` | ✅ 纯算法 |
| `playback/MeloXPlaybackService.kt` | `Core/Playback/Engine/` | ❌ Media3 → 需重写 |
| `ui/` | `Features/` | ❌ Compose → 需重写 |
| `platform/floating/` | `Features/FloatingLyrics/` | ❌ Android Service → 需重写 |

---

## 四、Android 代码中可直接用于 Windows 的模块（纯 Kotlin，零平台依赖）

以下模块**不依赖任何 Android API**，直接复制即可在 JVM/Dual 上运行：

| 文件 | 行数 | 功能 | 对应 iOS |
|------|------|------|---------|
| `core/lyrics/LyricModels.kt` | 361 | 歌词领域模型 + LRC/YRC 解析 | `LyricParser.swift` |
| `core/lyrics/TtmlLyricsParser.kt` | 226 | TTML/AMLL 解析 | `TTMLLyricParser.swift` |
| `core/lyrics/AmlldbLyricsClient.kt` | 25 | AMLL 数据库客户端 | `LyricSourceMerger.swift` |
| `core/lyrics/LyricBindingStore.kt` | ~200 | 歌词来源绑定持久化 | `QQMusicLyricMatchStore.swift` |
| `core/lyrics/LyricTimelineProcessor.kt` | ~300 | 歌词时间线处理 | `LyricPlaybackTimeline.swift` |
| `core/music/model/MusicModels.kt` | ~400 | 音乐领域模型 | `MusicModels.swift` |
| `core/music/provider/*.kt` | ~800 | Provider 抽象 + 能力契约 | iOS 单源架构 |
| `core/provider/netease/NeteaseProvider.kt` | ~300 | 网易云 Provider | `NeteaseAPI.swift` |
| `core/provider/qqmusic/*.kt` | ~600 | QQ音乐 Provider | `QQMusicLyricDecoder.swift` |
| `core/provider/kugou/*.kt` | ~800 | 酷狗 Provider | — |
| `core/network/NeteaseSearchClient.kt` | ~200 | 网易云搜索 | `NeteaseAPI+Home.swift` |
| `core/network/NeteaseAuthenticatedEapi.kt` | ~150 | EAPI 签名 | iOS 等价实现 |
| `core/network/NeteaseAuthenticatedWeapi.kt` | ~150 | WEAPI 签名 | iOS 等价实现 |
| `playback/MeloXAutoMix.kt` | ~400 | AutoMix 算法 | `AutoMixPlaybackCoordinator.swift` |
| `playback/MeloXAutoMixAudioAnalyzer.kt` | ~200 | 音频分析 | `PlaybackBeatTimeline.swift` |
| `playback/MeloXAutoMixEqualizerEnvelope.kt` | ~150 | EQ 包络 | `Equalizer/` |
| `playback/MeloXEqualizerController.kt` | ~200 | 均衡器控制 | `Equalizer/` |
| `playback/ProviderPlaybackResolver.kt` | ~250 | 播放地址解析 | `AudioPlaybackItemFactory.swift` |
| `playback/ChkszPlaybackResolver.kt` | ~150 | CHKSZ 解析 | — |
| `playback/MeloXAudioReactiveRuntime.kt` | ~200 | 音频响应运行时 | `PlaybackAudioSpectrum.swift` |
| `core/audio/MusicQuality*.kt` | ~200 | 音质模型 | `MusicQuality.swift` |
| `core/recognition/*.kt` | ~300 | 听歌识曲 | `SongRecognition/` |
| `core/recommendation/*.kt` | ~300 | 推荐引擎 | — |
| `core/update/MeloXUpdateClient.kt` | ~100 | 版本检查 | `Updates/` |
| `core/diagnostics/MeloXLogExporter.kt` | ~100 | 日志导出 | — |
| `core/download/MeloXDownloadStore.kt` | ~300 | 下载存储 | `DownloadStore.swift` |

**合计：约 8,000-10,000 行纯 Kotlin 代码，零平台依赖，可直接用于 Windows。**

---

## 五、Windows 移植策略

### 5.1 核心思路：参考 macOS Desktop 模式，用 Android 的 Kotlin 代码替代 iOS Swift Core

```
MeloX-Windows
├── shared/                      # 共享层（从 Android core/ 抽取）
│   ├── commonMain/              # 纯 Kotlin，无平台依赖
│   │   ├── lyrics/              # 歌词引擎
│   │   ├── music/               # 模型 + Provider
│   │   ├── network/             # 网络（Ktor 代替 OkHttp）
│   │   ├── playback/            # 播放算法（解析、AutoMix、EQ）
│   │   ├── audio/               # 音质模型
│   │   ├── recognition/         # 识曲
│   │   ├── recommendation/      # 推荐
│   │   └── ...
│   └── desktopMain/             # Desktop 平台实现
│       ├── AudioEngine.kt       # JVM 音频后端
│       ├── Storage.kt           # 文件存储
│       └── ...
├── desktopApp/                  # Windows 应用层
│   ├── MeloXWindowsApp.kt       # @main 入口
│   ├── WindowsAppModel.kt       # 包装共享层的 Model
│   ├── WindowsRootView.kt       # 根视图
│   ├── WindowsCommands.kt       # 全局快捷键
│   ├── WindowsMiniPlayer.kt     # MiniPlayer 窗口
│   ├── WindowsFloatingLyrics.kt # 悬浮歌词窗口
│   └── ...
├── ui/                          # Compose Multiplatform Desktop UI
│   ├── player/                  # 播放器 UI
│   ├── library/                 # 音乐库 UI
│   ├── search/                  # 搜索 UI
│   ├── settings/                # 设置 UI
│   └── ...
└── build.gradle.kts             # Compose Multiplatform Desktop 配置
```

### 5.2 为什么这是最小成本路径

| 对比 | 从零写 | 用 Android Kotlin 代码 |
|------|--------|---------------------|
| 歌词引擎 | 重写 LRC/YRC/TTML 解析（~2000行） | 复制 `core/lyrics/`，零改动 |
| 多源聚合 | 重写 AMLL→QQ→NetEase 聚合（~800行） | 复制 `MeloXProviderLyricsLoader.kt`，零改动 |
| 音乐模型 | 重写 Song/Album/Artist（~400行） | 复制 `core/music/model/`，零改动 |
| Provider 实现 | 重写网易云/QQ/酷狗签名（~3000行） | 复制 `core/provider/`，只换 HTTP 库 |
| 播放算法 | 重写 AutoMix/EQ（~1000行） | 复制 `playback/`，零改动 |
| UI | 重写所有页面（~30000行） | 用 Compose Multiplatform，参考 macOS Desktop 布局 |
| 音频后端 | 选型 + 实现（~1000行） | 用 JVM 音频库 |

**结论：约 8,000-10,000 行代码直接复用，只需重写 UI 和音频后端（约 15,000-20,000 行）。**

---

## 六、具体实施步骤

### 阶段一：抽取共享模块（3-5 天）

1. 在 `MeloX-Windows/` 下创建 `shared/commonMain/` 目录
2. 将 Android `core/` 下纯 Kotlin 文件**复制**（不改逻辑）到 `commonMain`：
   - `core/lyrics/*.kt` → `shared/commonMain/lyrics/`
   - `core/music/model/*.kt` → `shared/commonMain/music/model/`
   - `core/music/provider/*.kt` → `shared/commonMain/music/provider/`
   - `core/provider/netease/*.kt` → `shared/commonMain/provider/netease/`
   - `core/provider/qqmusic/*.kt` → `shared/commonMain/provider/qqmusic/`
   - `core/provider/kugou/*.kt` → `shared/commonMain/provider/kugou/`
   - `core/network/Netease*.kt` → `shared/commonMain/network/`（OkHttp 换 Ktor）
   - `core/audio/*.kt` → `shared/commonMain/audio/`
   - `playback/MeloXAutoMix*.kt` → `shared/commonMain/playback/`
   - `playback/MeloXEqualizer*.kt` → `shared/commonMain/playback/`
   - `playback/ProviderPlaybackResolver.kt` 等 → `shared/commonMain/playback/`
3. 改 import：`okhttp3.*` → `io.ktor.client.*`
4. Android 侧保留引用（改为依赖 shared 模块）或继续独立持有

### 阶段二：音频后端（5-7 天）

Windows 没有 Media3。JVM 生态选项：

| 方案 | 优点 | 缺点 |
|------|------|------|
| **Juce (Kotlin/JVM)** | 功能最全，支持缓存/预加载/淡入淡出 | 学习曲线 |
| **Tritone + javax.sound** | 轻量，纯 JVM | 功能有限 |
| **mpv (dkit 等)** | 成熟音频引擎 | 绑定可能不完整 |

推荐：**Juce**（功能最接近 Media3，支持 AutoMix 双播放器预载）

### 阶段三：Desktop UI（10-15 天）

用 Compose Multiplatform Desktop 实现，参考 macOS Desktop 的布局设计：

- `WindowsRootView.kt` — 根导航（首页/发现/音乐库/设置/搜索）
- `player/` — 播放器 UI（全屏 + Mini + 歌词 + 队列）
- `library/` — 音乐库
- `search/` — 搜索
- `settings/` — 设置
- `WindowsMiniPlayer.kt` — MiniPlayer 独立窗口
- `WindowsFloatingLyrics.kt` — 悬浮歌词窗口

### 阶段四：平台能力（3-5 天）

- 窗口管理（主窗口 + MiniPlayer + 悬浮歌词）
- 系统托盘图标
- 全局快捷键
- 文件存储（`Windows.Storage` 或 JVM 文件 API）
- 通知（Windows Toast）

### 阶段五：打包（2-3 天）

- Gradle Compose Multiplatform Desktop target
- `jpackage` 生成 Windows installer (.exe)
- 签名

---

## 七、成本估算

| 阶段 | 内容 | 工作量 |
|------|------|--------|
| 阶段一 | 抽取共享模块 | 3-5 天 |
| 阶段二 | 音频后端 | 5-7 天 |
| 阶段三 | Desktop UI | 10-15 天 |
| 阶段四 | 平台能力 | 3-5 天 |
| 阶段五 | 打包调试 | 2-3 天 |
| **总计** | | **23-35 天（1人）** |

---

## 八、风险

1. **音频后端是最大技术风险** — Media3 功能非常完整（缓存、预加载、淡入淡出、Session），JVM 替代方案需要仔细选型
2. **QQ/酷狗的签名算法** — 涉及平台特定逻辑，可能需要保留 Android 侧实现或寻找替代
3. **AutoMix 双播放器预载** — 需要两个独立音频实例，JVM 方案需验证可行性
4. **悬浮歌词窗口** — Windows 上无边框透明窗口的实现需要测试
5. **许可证** — GPLv3，需保留上游署名