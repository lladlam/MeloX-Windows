# MeloX-Windows — 移植总结

## 一句话结论

**用 Compose Multiplatform Desktop，把 Android 已有的 13,071 行纯 Kotlin 业务代码直接搬到 Windows，只重写 UI 和音频后端。参考 macOS Desktop 的适配模式（在 Core 之上加 Desktop 适配层），23-35 天可完成最小可用版本。**

---

## 核心路径

```
MeloX-Android (Kotlin + Compose)
        │
        ├─ 纯 Kotlin 业务代码 ──→ shared/commonMain ──→ Windows 直接复用
        │   (歌词/模型/Provider/算法)     (~10,000 行)        (零改动)
        │
        ├─ Android UI ──→ Compose Multiplatform Desktop UI
        │   (~34,000 行)    (~15,000 行重写，语法 80% 相同)
        │
        └─ Media3 音频 ──→ JVM 音频后端 (mpv 或 VLCJ)
            (~2,000 行)     (~1,000 行重写)
```

---

## 为什么这是最小成本

1. **Android 的歌词引擎已经是纯 Kotlin 的** — `TtmlLyricsParser.kt`、`MeloXProviderLyricsLoader.kt` 零平台依赖，复制即用
2. **Android 的 Provider 抽象干净** — 网易云/QQ/酷狗/Spotify/Jellyfin/B站，换 HTTP 库即可
3. **Compose Multiplatform 语法与 Android Compose 80% 相同** — 你已有 Android Compose 经验，学习成本接近零
4. **macOS Desktop 已有可参考的适配模式** — `DesktopAppModel` 包装 Core Store，Desktop 专属功能封装在 `Desktop*` 文件中，Windows 完全可以照搬

---

## 三个平台的关系

| 平台 | 开发者 | 定位 |
|------|--------|------|
| MeloX iOS | moye | 原版，Core 是 Swift |
| MeloXDesktop | moye | macOS 桌面版，在 iOS Core 上加 Desktop 适配层 |
| MeloX-Android | 用户 | Android 移植版，Core 重写为 Kotlin |

**Windows 的定位应该和 macOS Desktop 一致**：在已有 Core（Android 的 Kotlin 版本）之上，加一个 Windows 适配层。

---

## 文件清单

| 文件 | 内容 |
|------|------|
| `01-三平台架构对比.md` | iOS / Desktop / Android 完整架构分析，代码规模对比，可移植性评估 |
| `02-文件级移植清单.md` | 215 个文件的逐文件移植动作（复制/改import/重写/新建） |
| `03-移植实施计划.md` | 6 个阶段的详细实施步骤、代码示例、里程碑 |
| `04-总结.md` | 本文件 |

---

## 关键数字

| 指标 | 数值 |
|------|------|
| 总文件分析 | 215 个 |
| 可直接复制 | 79 个文件 / ~13,071 行 |
| 改 import 可用 | 72 个文件 / ~15,000 行 |
| 小改可用 | ~30 个文件 / ~2,000 行 |
| 需重写 | ~30 个文件 / ~28,000 行（主要是 UI） |
| 新建 | ~15 个文件 / ~3,000 行 |
| **业务逻辑复用率** | **~85%** |
| **总代码复用率** | **~32%** |
| 预估工作量 | **23-35 天（1人）** |

---

## 下一步

按 `03-移植实施计划.md` 的阶段一，先搭建 Gradle 工程，然后从 `shared/commonMain/lyrics/` 开始复制文件。